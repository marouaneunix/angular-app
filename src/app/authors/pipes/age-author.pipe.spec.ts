import { AgeAuthorPipe } from './age-author.pipe';

describe('AgeAuthorPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeAuthorPipe();
    expect(pipe).toBeTruthy();
  });
});
